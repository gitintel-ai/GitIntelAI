name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ════════════════════════════════════════════════════════════════
  # CLI (Rust)
  # ════════════════════════════════════════════════════════════════
  cli:
    name: CLI Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/cli

    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: packages/cli

      - name: Check formatting
        run: cargo fmt --check

      - name: Clippy lints
        run: cargo clippy -- -D warnings

      - name: Run tests
        run: cargo test --all-features

      - name: Build release
        run: cargo build --release

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: gitintel-linux-amd64
          path: packages/cli/target/release/gitintel

  # ════════════════════════════════════════════════════════════════
  # API Server (Bun)
  # ════════════════════════════════════════════════════════════════
  server:
    name: Server Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/server

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_PASSWORD: test
          POSTGRES_DB: gitintel_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies (workspace root for linking)
        run: cd ${{ github.workspace }} && bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Type check
        continue-on-error: true  # TODO: fix pre-existing drizzle ORM type errors
        run: bun run type-check

      - name: Run tests
        run: bun test
        env:
          DATABASE_URL: postgresql://postgres:test@localhost:5432/gitintel_test

  # ════════════════════════════════════════════════════════════════
  # Dashboard (Next.js)
  # ════════════════════════════════════════════════════════════════
  dashboard:
    name: Dashboard Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/dashboard

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies (workspace root for linking)
        run: cd ${{ github.workspace }} && bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Type check
        continue-on-error: true  # TODO: fix pre-existing Next.js type errors
        run: bun run type-check

      - name: Build
        continue-on-error: true  # TODO: depends on type check fixes
        run: bun run build
        env:
          NEXT_PUBLIC_API_URL: http://localhost:3001
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_test_placeholder

  # ════════════════════════════════════════════════════════════════
  # Dashboard E2E (Playwright)
  # ════════════════════════════════════════════════════════════════
  e2e:
    name: Dashboard E2E
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/dashboard

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies (workspace root for linking)
        run: cd ${{ github.workspace }} && bun install --frozen-lockfile

      - name: Install Playwright browsers
        run: bunx playwright install --with-deps chromium firefox

      - name: Build dashboard
        continue-on-error: true  # TODO: depends on type check fixes
        run: bun run build
        env:
          NEXT_PUBLIC_API_URL: http://localhost:3001
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_test_placeholder

      - name: Run E2E tests
        run: bunx playwright test --project=chromium --project=firefox
        env:
          CI: true

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: packages/dashboard/playwright-report/
          retention-days: 14

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-results
          path: packages/dashboard/test-results/
          retention-days: 14

  # ════════════════════════════════════════════════════════════════
  # Integration E2E (Playwright + real API + Postgres)
  # ════════════════════════════════════════════════════════════════
  e2e-integration:
    name: Integration E2E
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_PASSWORD: test
          POSTGRES_DB: gitintel_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies (workspace root for linking)
        run: bun install --frozen-lockfile

      - name: Push DB schema
        run: cd packages/server && bunx drizzle-kit push
        env:
          DATABASE_URL: postgresql://postgres:test@localhost:5432/gitintel_test

      - name: Seed test data
        run: cd packages/dashboard && bun run e2e-integration/seed.ts
        env:
          DATABASE_URL: postgresql://postgres:test@localhost:5432/gitintel_test

      - name: Build dashboard
        continue-on-error: true  # TODO: depends on type check fixes
        run: cd packages/dashboard && bun run build
        env:
          NEXT_PUBLIC_API_URL: http://localhost:3001
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_test_placeholder

      - name: Install Playwright browsers
        run: cd packages/dashboard && bunx playwright install --with-deps chromium

      - name: Run integration E2E tests
        run: cd packages/dashboard && bunx playwright test --config=playwright.integration.config.ts
        env:
          CI: true
          DATABASE_URL: postgresql://postgres:test@localhost:5432/gitintel_test
          NODE_ENV: test

      - name: Upload integration report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report-integration
          path: packages/dashboard/playwright-report-integration/
          retention-days: 14

  # ════════════════════════════════════════════════════════════════
  # Core Library
  # ════════════════════════════════════════════════════════════════
  core:
    name: Core Library
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/core

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies (workspace root for linking)
        run: cd ${{ github.workspace }} && bun install --frozen-lockfile

      - name: Type check
        run: bun run type-check

      - name: Run tests
        run: bun test

  # ════════════════════════════════════════════════════════════════
  # Preview Deploy (gated on E2E passing)
  # ════════════════════════════════════════════════════════════════
  preview-deploy:
    name: Preview Deploy
    runs-on: ubuntu-latest
    needs: [e2e, e2e-integration]
    if: github.event_name == 'pull_request'

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies (workspace root for linking)
        run: bun install --frozen-lockfile

      - name: Build
        continue-on-error: true
        run: cd packages/dashboard && bun run build
        env:
          NEXT_PUBLIC_API_URL: https://api-preview.gitintel.com
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.CLERK_PUBLISHABLE_KEY_PREVIEW }}

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: gitintel-dashboard
          directory: packages/dashboard/.next
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}

      - name: Comment PR
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## Preview Deployment\n\nYour preview is ready!\n\n**Dashboard:** https://gitintel-dashboard-${context.payload.pull_request.number}.pages.dev\n\n_Deployed after all E2E tests passed._`
            })

  # ════════════════════════════════════════════════════════════════
  # Docker Build
  # ════════════════════════════════════════════════════════════════
  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [cli, server, dashboard, e2e, e2e-integration, core]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build CLI image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: infra/docker/Dockerfile.cli
          push: false
          tags: gitintel/cli:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build Server image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: infra/docker/Dockerfile.server
          push: false
          tags: gitintel/server:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build Dashboard image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: infra/docker/Dockerfile.dashboard
          push: false
          tags: gitintel/dashboard:test
          cache-from: type=gha
          cache-to: type=gha,mode=max
